var username= document.forms['forms']['username'];
var password= document.forms['forms']['password'];
function validated()
{ if (username.value.lenght <9)
    username.style.border
    {
        return false;
    }

}